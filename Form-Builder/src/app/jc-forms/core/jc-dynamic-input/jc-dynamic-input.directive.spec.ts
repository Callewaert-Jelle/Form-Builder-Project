import { JcDynamicInputDirective } from './jc-dynamic-input.directive';

describe('JcDynamicInputDirective', () => {
  it('should create an instance', () => {
    const directive = new JcDynamicInputDirective();
    expect(directive).toBeTruthy();
  });
});
